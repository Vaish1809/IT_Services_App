import 'package:flutter/material.dart';

class ProceedtoPay extends StatelessWidget {
  const ProceedtoPay({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text("Proceed to pay")),
    );
  }
}